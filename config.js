// Điền hai giá trị dưới đây từ Supabase, mục Project Settings > API.
// anon key được phép để công khai trong mã nguồn, đây không phải bí mật.
window.SB_CONFIG = {
  url: "https://<mã-dự-án>.supabase.co",
  anonKey: "<anon-key>"
};
